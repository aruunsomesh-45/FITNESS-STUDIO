# Supabase Configuration
# Copy this file to .env.local and fill in your actual values

# Public keys (safe to expose to client - these are embedded in the client bundle)
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# SECRET - Server-only keys (NEVER commit these!)
# This key bypasses Row Level Security (RLS) - use with extreme caution
# Get from: Supabase Dashboard > Project Settings > API > service_role key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Production Domain
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# How to get your Supabase keys:
# 1. Go to https://supabase.com/dashboard
# 2. Select your project
# 3. Navigate to Settings > API
# 4. Copy the values:
#    - Project URL  NEXT_PUBLIC_SUPABASE_URL
#    - anon/public  NEXT_PUBLIC_SUPABASE_ANON_KEY
#    - service_role  SUPABASE_SERVICE_ROLE_KEY (SECRET!)
